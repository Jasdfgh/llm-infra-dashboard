# Core dependencies for Module 1 (Signal Ingestion) + Module 3 (Infra)
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.0
PyJWT[crypto]>=2.8.0

# MCP service runtime
mcp>=1.27.0
uvicorn>=0.46.0

# Optional - future async SQLite support (not required for MVP)
# aiosqlite>=0.20.0

# Optional - precise token counting (MVP uses fast estimate len/4)
# tiktoken>=0.7.0

# Optional - scheduler (Week 2, MVP uses CLI manual trigger)
# apscheduler>=3.10.0

# Dev / tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
